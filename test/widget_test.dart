import 'package:flutter_test/flutter_test.dart';

// Widget tests will be added in Plan 01-02 once the auth flow is implemented.
void main() {
  test('placeholder', () {
    // intentionally empty — replaced by real tests in subsequent plans
    expect(true, isTrue);
  });
}
