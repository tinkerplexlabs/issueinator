---
phase: 02-bug-report-read-path
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - lib/presentation/screens/report_detail_screen.dart
autonomous: false
requirements:
  - DETL-01
  - DETL-02
  - DETL-03

must_haves:
  truths:
    - "Developer can tap a report in the list and see full detail: description, device_info, app_version, platform, logs"
    - "Screenshot renders inline from base64 and is zoomable via InteractiveViewer"
    - "GitHub issue link is tappable and opens in external browser when present"
    - "Triage tag placeholder shows 'Not yet triaged' (Phase 3 will replace)"
    - "End-to-end flow works on device: dashboard → list → detail with screenshot"
  artifacts:
    - path: "lib/presentation/screens/report_detail_screen.dart"
      provides: "Full report detail with screenshot rendering"
      contains: "InteractiveViewer"
  key_links:
    - from: "lib/presentation/screens/report_detail_screen.dart"
      to: "lib/infrastructure/repositories/bug_report_repository.dart"
      via: "GetIt → BugReportRepository.getReportDetail()"
      pattern: "getReportDetail"
    - from: "lib/presentation/screens/report_detail_screen.dart"
      to: "url_launcher"
      via: "launchUrl for github_issue_url"
      pattern: "launchUrl"
---

<objective>
Create the report detail screen showing full bug report data including zoomable screenshot, then verify the complete end-to-end read path on a physical device.

Purpose: Complete the read path so the developer can inspect any bug report in full detail before triaging it in Phase 3.
Output: ReportDetailScreen and verified end-to-end flow (dashboard → list → detail).
</objective>

<execution_context>
@/home/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/home/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bug-report-read-path/02-RESEARCH.md
@.planning/phases/02-bug-report-read-path/02-01-SUMMARY.md
@.planning/phases/02-bug-report-read-path/02-02-SUMMARY.md
@lib/infrastructure/repositories/bug_report_repository.dart
@lib/domain/models/bug_report_detail.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ReportDetailScreen with screenshot and metadata</name>
  <files>lib/presentation/screens/report_detail_screen.dart</files>
  <action>
Create `lib/presentation/screens/report_detail_screen.dart`:

**Constructor:** Takes `required String reportId`.

**State management:**
- In `initState`, fetch detail: call `GetIt.instance<BugReportRepository>().getReportDetail(reportId)`
- Store result in local state (this screen does NOT need a shared controller — detail is a one-off fetch)
- State fields: `BugReportDetail? _detail`, `bool _isLoading = true`, `String? _error`

**AppBar:**
- Title: "Report Detail" (or report ID truncated)

**Body — three states:**
- **Loading:** `Center(child: CircularProgressIndicator())`
- **Error:** Error text with "Retry" button
- **Data:** `SingleChildScrollView` with the following sections in a `Column`:

**Section 1: Status bar**
- Triage status: Show "Not yet triaged" with a grey chip/badge — comment: `// Phase 3: replace with actual triage tag from bug_report_triage table`
- GitHub link: If `githubIssueUrl != null`, show a tappable link chip with icon `Icons.open_in_new`. Use `url_launcher` package (`launchUrl(Uri.parse(url))`). If null, show "Not synced to GitHub" in grey.

**Section 2: Core fields (use a consistent label-value layout)**
- Description (full text, not truncated)
- Platform
- App Version
- Created At (formatted with `DateFormat('MMMM d, yyyy h:mm a')`)

**Section 3: Device info**
- Header "Device Info"
- Show `deviceInfo` in a monospaced font container (like a code block) — use `Container` with a dark background and `SelectableText` with a monospace `TextStyle`

**Section 4: Logs**
- Header "Logs"
- Show `logs` in a monospaced scrollable container (same style as device info)
- If logs is null or empty, show "No logs available"

**Section 5: Screenshot**
- Header "Screenshot"
- If `screenshotBase64` is not null:
  - Decode: `final bytes = base64Decode(detail.screenshotBase64!);` (import `dart:convert`)
  - Wrap in `FutureBuilder` or do the decode in a `Future.microtask` to avoid blocking build():
    ```dart
    // Decode async to avoid blocking UI thread
    late Future<Uint8List> _screenshotFuture;
    // In data-loaded callback:
    _screenshotFuture = Future.microtask(() => base64Decode(detail.screenshotBase64!));
    ```
  - Render: `InteractiveViewer(child: Image.memory(bytes))` — allows pinch-to-zoom
  - Constrain height: wrap in `ConstrainedBox(constraints: BoxConstraints(maxHeight: 400))`
- If null, show "No screenshot available"

**Imports:** `dart:convert`, `package:flutter/material.dart`, `package:get_it/get_it.dart`, `package:intl/intl.dart`, `package:url_launcher/url_launcher.dart`, project models and repository.
  </action>
  <verify>
    <automated>cd /home/daniel/work/tinkerplexlabs/demos/issueinator && flutter analyze lib/presentation/screens/report_detail_screen.dart 2>&1 | tail -5</automated>
  </verify>
  <done>ReportDetailScreen compiles cleanly. Shows full description, device_info, app_version, platform, logs. Screenshot decodes from base64 and renders in InteractiveViewer. GitHub link opens via url_launcher. Triage placeholder displayed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify end-to-end read path on device</name>
  <files>lib/presentation/screens/report_detail_screen.dart</files>
  <action>
Human verification checkpoint. Claude builds and deploys to device: `flutter run`.
What was built: Complete bug report read path — dashboard with product counts, report list with pull-to-refresh, report detail with zoomable screenshot.
  </action>
  <verify>
    <manual>
1. Build and run on device: `flutter run`
2. Sign in with Google (should auto-sign-in if session persists from Phase 1)
3. **Dashboard check:** Verify you see product cards with total/unprocessed counts. Expect freecell (46 reports, ~8 unprocessed) and puzzle_nook (14 reports). issueinator should show 0/0.
4. **Tap freecell card** — should navigate to report list
5. **Report list check:** Verify scrollable list with description previews, platform labels, dates, and synced/unprocessed indicators
6. **Pull-to-refresh:** Pull down on the list — should show refresh indicator and reload
7. **Tap any report** — should navigate to detail screen
8. **Report detail check:** Verify full description, device info, logs, and screenshot all render
9. **Screenshot zoom:** Pinch-to-zoom on screenshot should work via InteractiveViewer
10. **GitHub link (if present):** Tap the GitHub issue link — should open in external browser
11. **Back navigation:** Press back from detail → list → dashboard — all navigation smooth
    </manual>
  </verify>
  <done>Developer confirms full read path works on device: dashboard counts correct, list shows reports with metadata, detail renders screenshot with zoom, GitHub links open in browser.</done>
</task>

</tasks>

<verification>
- ReportDetailScreen compiles without errors
- Full end-to-end navigation: dashboard → list → detail → back works on device
- Screenshot renders and is zoomable
- All Phase 2 requirements (DASH-01 through DETL-03) are satisfied
</verification>

<success_criteria>
- Developer can navigate from dashboard to any report's full detail
- Screenshot renders inline from base64 with pinch-to-zoom
- GitHub link opens in browser when present
- Triage tag shows placeholder (ready for Phase 3)
- All 10 Phase 2 requirements verified on device
</success_criteria>

<output>
After completion, create `.planning/phases/02-bug-report-read-path/02-03-SUMMARY.md`
</output>
