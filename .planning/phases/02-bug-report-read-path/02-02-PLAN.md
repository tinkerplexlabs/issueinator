---
phase: 02-bug-report-read-path
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - lib/presentation/screens/home_screen.dart
  - lib/presentation/screens/report_list_screen.dart
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - LIST-01
  - LIST-02
  - LIST-03
  - LIST-04

must_haves:
  truths:
    - "Home screen shows per-product card with total and unprocessed report counts"
    - "Tapping a product card navigates to report list filtered by that product"
    - "Report list shows scrollable list items with description preview, platform, date, and triage status"
    - "Pull-to-refresh on report list reloads data from Supabase"
    - "Products with 0 reports display gracefully (issueinator shows 0/0)"
  artifacts:
    - path: "lib/presentation/screens/home_screen.dart"
      provides: "Dashboard with product count cards"
      contains: "DashboardController"
    - path: "lib/presentation/screens/report_list_screen.dart"
      provides: "Filterable report list with pull-to-refresh"
      contains: "RefreshIndicator"
  key_links:
    - from: "lib/presentation/screens/home_screen.dart"
      to: "lib/application/controllers/dashboard_controller.dart"
      via: "GetIt.instance<DashboardController>()"
      pattern: "DashboardController"
    - from: "lib/presentation/screens/home_screen.dart"
      to: "lib/presentation/screens/report_list_screen.dart"
      via: "Navigator.push on product card tap"
      pattern: "Navigator\\.push.*ReportListScreen"
    - from: "lib/presentation/screens/report_list_screen.dart"
      to: "lib/application/controllers/report_list_controller.dart"
      via: "GetIt.instance<ReportListController>()"
      pattern: "ReportListController"
---

<objective>
Rewrite the HomeScreen as a dashboard showing per-product report counts, and create the ReportListScreen showing a scrollable, pull-to-refresh list of reports for a selected product.

Purpose: Give the developer a quick overview of bug report volume per product and the ability to browse individual reports.
Output: Rewritten home_screen.dart and new report_list_screen.dart.
</objective>

<execution_context>
@/home/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/home/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bug-report-read-path/02-RESEARCH.md
@.planning/phases/02-bug-report-read-path/02-01-SUMMARY.md
@lib/presentation/screens/home_screen.dart
@lib/config/dependencies.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite HomeScreen as product dashboard</name>
  <files>lib/presentation/screens/home_screen.dart</files>
  <action>
Rewrite `home_screen.dart` to serve as the bug report dashboard. KEEP the existing AppBar with logout button and the GitHub integration section. REPLACE the products ListView with a dashboard grid of product count cards.

**Structure:**
- On `initState`, load products from Supabase (keep existing `_loadProducts()` query for product names), then call `DashboardController.loadCounts()` with the product name list.
- Use `ListenableBuilder` wrapping `DashboardController` for reactive count display.

**Dashboard layout:**
- Keep AppBar with "IssueInator" title and logout IconButton
- Keep GitHub Integration section (ListenableBuilder with connect button) exactly as-is
- REMOVE the "Developer UID" text and "Products table columns" text (debug info no longer needed)
- Replace the products ListView with a grid/list of product cards:

**Each product card (use Card widget with InkWell):**
- Product name as title (capitalize first letter)
- "X reports" as subtitle showing total count
- "Y unprocessed" in a secondary text or badge (use a warning color like `Colors.orange` if unprocessed > 0, or `Colors.green` if 0)
- `onTap`: Navigate to `ReportListScreen(productName: name)`

**Navigation:**
```dart
Navigator.push(context, MaterialPageRoute(
  builder: (_) => ReportListScreen(productName: count.productName),
));
```

**Loading state:** Show CircularProgressIndicator while DashboardController.isLoading is true.
**Error state:** Show error message with retry button if DashboardController.error is non-null.

Import `ReportListScreen`, `DashboardController`, `GetIt`.
  </action>
  <verify>
    <automated>cd /home/daniel/work/tinkerplexlabs/demos/issueinator && flutter analyze lib/presentation/screens/home_screen.dart 2>&1 | tail -5</automated>
  </verify>
  <done>HomeScreen shows product cards with total/unprocessed counts. Tapping a card navigates to ReportListScreen. GitHub integration section preserved. Debug text removed.</done>
</task>

<task type="auto">
  <name>Task 2: Create ReportListScreen with pull-to-refresh</name>
  <files>lib/presentation/screens/report_list_screen.dart</files>
  <action>
Create `lib/presentation/screens/report_list_screen.dart`:

**Constructor:** Takes `required String productName` parameter.

**Lifecycle:**
- `initState`: call `GetIt.instance<ReportListController>().loadReports(productName)`

**AppBar:**
- Title: product name (capitalized)
- No additional actions needed

**Body (three states):**
- **Loading:** `Center(child: CircularProgressIndicator())` when `controller.isLoading` and reports list is empty
- **Error:** `Center` with error text and "Retry" button calling `controller.loadReports(productName)`
- **Data:** `RefreshIndicator` wrapping `ListView.builder`

**RefreshIndicator:**
- `onRefresh: () => controller.refresh()`
- Wraps the entire ListView

**Each list item (ListTile or custom widget):**
- **Title:** `report.descriptionPreview` (the truncated description getter from BugReportSummary)
- **Subtitle row 1:** Platform chip/text + formatted date (`DateFormat('MMM d, yyyy').format(report.createdAt)`)
- **Subtitle row 2 / trailing:** Triage status indicator:
  - If `githubIssueUrl != null`: green icon + "Synced" text
  - If `githubIssueUrl == null`: grey icon + "Unprocessed" text (Phase 2 proxy; comment: `// Phase 2 proxy: replace with triage tag display in Phase 3`)
- **onTap:** `Navigator.push` to `ReportDetailScreen(reportId: report.id)` — import ReportDetailScreen but it won't exist yet; use a forward reference comment: `// TODO(02-03): ReportDetailScreen navigation - implement in plan 02-03`
  - For now, navigate with a placeholder: show a SnackBar saying "Detail view coming in next plan" OR if you can import a not-yet-created file, just add the Navigator.push with the import — the file will be created in plan 02-03.
  - **Preferred approach:** Add the `Navigator.push` to `ReportDetailScreen(reportId: report.id)` and include the import. Plan 02-03 will create the file. This creates a temporary analysis warning but keeps the wiring correct.

**Use ListenableBuilder** wrapping `GetIt.instance<ReportListController>()` for the body.

**Empty state:** If reports list is empty and not loading, show "No reports for this product" centered text.

Import `intl` for DateFormat, `get_it` for GetIt.
  </action>
  <verify>
    <automated>cd /home/daniel/work/tinkerplexlabs/demos/issueinator && flutter analyze lib/presentation/screens/report_list_screen.dart 2>&1 | tail -10</automated>
    <manual>Analysis may show a warning for ReportDetailScreen import (file created in 02-03) — this is expected and acceptable.</manual>
  </verify>
  <done>ReportListScreen shows scrollable list of reports with description preview, platform, date, and triage status. Pull-to-refresh calls controller.refresh(). Empty state handled. Navigation to detail wired.</done>
</task>

</tasks>

<verification>
- `flutter analyze` passes on home_screen.dart with zero errors
- HomeScreen uses DashboardController (not raw Supabase queries) for counts
- ReportListScreen wraps ListView in RefreshIndicator
- Each list item shows: description preview, platform, date, triage status (DASH-01, DASH-02, DASH-03, LIST-01, LIST-02, LIST-04 covered)
- No `select('*')` appears in any screen file (LIST-03: column projection enforced at repository layer)
</verification>

<success_criteria>
- Dashboard shows per-product total and unprocessed counts
- Tapping a product navigates to its report list
- Report list displays all required fields per item
- Pull-to-refresh reloads report data
- Products with 0 reports show gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/02-bug-report-read-path/02-02-SUMMARY.md`
</output>
